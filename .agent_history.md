# Agent history — Calendar 2.0

## 2026-09-14 — /MegaBrain Calendar 2.0

- **image_attachment:** false
- **docs:** RF do utilizador = spec; execução sob MegaBrain
- **Auditoria:**
  - `CalendarView.qml`: grelha mês, today highlight, ‹›, TODAY, Keys ←→ Esc
  - Células: `acceptedButtons: Qt.NoButton` — sem click-to-day
  - Sem eventos/persistência
  - Abertura: `togglePage("cal")` / clock / IPC `calendar`
  - Notificações: NotificationsView existente (reutilizar se countdown)
- **DECISÃO:** continuar — evoluir CalendarView + calendar.sh JSON local + estado em shell.qml

### Entrega
- `calendar.sh` + `calendar_api.sh` (Bonsai-ready)
- `shell.qml` cal* state partilhado
- `CalendarView.qml` MONTH/DAY/ADD + indicators + timeline + next + summary
- Navbar clock subtitle = próximo evento
- Testes: create/next/day/month/summary/api OK; qs_ok clean
